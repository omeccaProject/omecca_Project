# --- 필수 (Mock 모드로 전체 파이프라인 실행 가능) ---------------------------
fastapi>=0.110
uvicorn>=0.27          # [standard] 옵션은 httptools 컴파일이 필요해 제외 (성능 가속용일 뿐)
pydantic>=2.5
PyYAML>=6.0
websockets>=12.0       # WebSocket 실시간 푸시 (순수 파이썬, 컴파일 불필요)

# --- 실 영상/모델 연동 시 ---------------------------------------------------
# opencv-python>=4.9          # 전처리(기울기 보정, CLAHE, 이진화)
# numpy>=1.26
# easyocr>=1.7                # 번호판 문자 인식
# ultralytics>=8.3            # YOLOv11 번호판 검출
# torch>=2.2                  # easyocr / ultralytics 백엔드

# --- MySQL 연동 시 (미설치 시 SQLite로 자동 폴백) ---------------------------
# pymysql>=1.1

# --- 개발/테스트 -------------------------------------------------------------
pytest>=8.0
httpx>=0.27

# 영상 처리 검증 및 벤치마크(bench_lpr.py)에 필요.
# 없으면 해당 테스트는 자동으로 건너뛴다.
opencv-python>=4.9
numpy>=1.26
pillow>=10.0
sqlglot>=25.0                 # MySQL 방언 검증용
