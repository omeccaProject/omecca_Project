# 오메카3 — 구현 설계 이유 정리 (b_gateway / b_dashboard / e_tracking)

작성: 장성혁 · 2026-08-17 · 팀원 공유용

이 문서는 "무엇을 만들었는지"가 아니라 **"왜 그 방식으로 만들었는지"**를 정리한 문서입니다.
코드만 보면 이해하기 어려운 설계 결정들, 특히 원인을 모르면 나중에 실수로 되돌리기 쉬운 부분들 위주로 적었습니다.

---

## 1. 인증을 두 체계로 나눈 이유 — JWT vs X-API-Key

게이트웨이에 접근하는 주체가 **두 종류**라서 인증도 두 체계로 나눴습니다.

- **사람(관제요원)** — 로그인해서 대시보드를 보는 사람. 이 사람들은 "누구인지"가 중요하고(로그인 기록, 관리자 승인 여부, 권한), 세션이 유지돼야 합니다. → **JWT**(`/api/auth/login`)
- **탐지 모듈(팀원 코드)** — YOLO/얼굴인식/LPR 등 사람이 아니라 프로그램이 이벤트를 쏘는 것. 이건 "누구인지"보다 "우리 팀 모듈이 맞는지"만 확인하면 되고, 로그인 세션 같은 게 필요 없습니다. → **X-API-Key**(고정 문자열, 전원 동일값)

만약 하나로 통일했다면, 팀원 모듈이 이벤트 하나 보낼 때마다 로그인 토큰을 발급받고 갱신하는 로직까지 각자 구현해야 했을 겁니다. 모듈은 "정해진 키만 헤더에 넣으면 끝"이 훨씬 간단해서 이렇게 나눴습니다.

`ApiKeyFilter`가 `/api/health`, `/api/auth/**`, `/api/admin/**`은 검사에서 빼놓은 이유도 같은 맥락입니다 — 헬스체크는 인증 자체가 필요 없고, 로그인/관리자 승인은 애초에 JWT 대상이라 X-API-Key로 이중 체크할 필요가 없기 때문입니다.

---

## 2. 이벤트가 실시간으로 뜨는 방식 — WebSocket(STOMP)을 쓴 이유

이벤트 리스트가 새로고침 없이 실시간으로 뜨려면 서버가 클라이언트에 "먼저 알려주는" 방식이 필요합니다. 여기서 흔히 쓰는 방법이 세 가지인데:

- **폴링(주기적으로 GET 반복)** — 구현은 제일 쉽지만, 이벤트가 없어도 계속 요청을 날려서 낭비고, 지연도 폴링 간격만큼 생깁니다.
- **Server-Sent Events** — 서버→클라이언트 단방향만 가능.
- **WebSocket** — 양방향, 지연 거의 없음.

WebSocket 중에서도 STOMP(Simple Text Oriented Messaging Protocol)를 얹은 이유는, Spring Boot가 `/topic/이름` 같은 "구독 채널" 개념을 기본으로 지원해서, 앞으로 이벤트 종류별로 채널을 나누고 싶어져도(예: `/topic/events/critical`만 따로 구독) 크게 바꿀 필요 없이 확장할 수 있기 때문입니다. 지금은 `/topic/events` 하나만 쓰지만, 구조 자체는 나중에 채널을 쪼개기 쉽게 짜여 있습니다.

프론트엔드가 SockJS를 같이 쓰는 이유는, 순수 WebSocket이 막혀있는 네트워크 환경(일부 공공기관 방화벽 등)에서도 자동으로 폴링 방식으로 폴백해주기 때문입니다. 지자체·공공기관 대상 서비스라 이 부분을 신경 썼습니다.

---

## 3. 이벤트 DTO를 "공통 필드 + meta" 구조로 만든 이유

`EventCreateRequest`가 `camId`/`eventType`/`bbox` 같은 고정 필드와, `meta`라는 자유 JSON 필드로 나뉘어 있는 이유는 **팀원 5명이 각자 다른 종류의 이벤트를 만들기 때문**입니다.

수배자 얼굴인식은 `faceMatchScore`가 필요하고, 음주운전 판정은 `zigzagCount`/`accelDecelCount`가 필요하고, 낙하물은 `stationaryDurationSec`가 필요합니다. 이 전부를 DB 테이블에 컬럼으로 만들면, 새 기능이 하나 추가될 때마다 테이블 구조를 바꿔야 하고(`ALTER TABLE`), 이미 쌓인 데이터와 호환도 신경 써야 합니다.

그래서 "모든 이벤트가 공통으로 갖는 필드"(카메라, 시각, 좌표, 신뢰도 등)만 고정 컬럼으로 두고, 이벤트 종류별로 다른 부분은 `meta`라는 JSON 컬럼 하나에 다 몰아넣었습니다. 새 이벤트 타입이 추가돼도 DB 스키마를 안 바꿔도 되고, 각 담당자가 자기 이벤트에 필요한 필드를 자유롭게 `meta`에 넣을 수 있습니다. 대신 "최상위 필드를 임의로 추가하지 말고 `meta`로 확장하라"는 규칙을 정한 것도 이 구조를 지키기 위해서입니다(이 규칙을 안 지킨 게 `a_core`의 `detectedClass` 케이스입니다).

---

## 4. Forza 데모 A→B→C→D 전환 버그, 왜 그 방식으로 고쳤는가

**증상**: A→B 구간 이동 중 A 영상이 무한 반복되고, B를 지나도 안 바뀌다가 C를 지나야 다음 영상으로 넘어감.

**원인을 찾는 과정에서 두 가지가 겹쳐 있었습니다.**

1. `UticCameraManager.selectRecord()`가 `force` 옵션을 아예 안 받고 있었습니다. `VideoManager.switchTo()`는 "이미 같은 카메라에 연결돼 있으면 재연결하지 않는다"는 최적화가 들어있는데(불필요한 재생 끊김 방지용), 구간이 바뀌어도 카메라 ID가 우연히 같으면 이 최적화 때문에 영상이 안 바뀌는 상황이 생깁니다. `force: true`를 강제로 넘겨야 하는데 그 옵션 자체가 함수에서 씹히고 있었습니다.
2. 구간 전환을 판단하는 로직이 "지금 영상 플레이어가 연결된 카메라 ID"를 기준으로 다음 전환 여부를 판단했는데, A→B로 넘어가는 그 첫 전환 시점에는 이 값이 아직 도착지(B)로 미리 표시돼 있는 경우가 있어서, "이미 B로 연결됐다"고 착각하고 실제 전환을 건너뛰는 off-by-one 버그가 있었습니다. 이게 하필 첫 전환(A→B)에서만 터지고 C부터는 멀쩡했던 이유입니다.

**고친 방법**: "지금 재생 중인 영상이 뭔지"로 판단하는 대신, "각 구간이 시작하는 바로 그 순간"(`beginDemoStageMovement`)에 그 구간 고유의 카메라 ID로 명시적으로 전환하도록 바꿨습니다. 애매하게 추론하는 대신 "이 시점엔 반드시 이 카메라"라고 확정적으로 정하는 방식이라, 타이밍이 어긋날 여지 자체를 없앤 겁니다.

---

## 5. 3분할 모니터 모드 — 왜 이렇게 설계했는가

### 왜 창을 3개가 아니라 2개만 여는가
처음엔 1번(대시보드)·2번·3번 모니터 각각에 새 창을 열었는데, 생각해보면 사용자가 버튼을 누른 그 창 자체가 이미 1번 모니터에 떠 있는 화면입니다. 굳이 똑같은 화면을 새 창으로 하나 더 띄울 이유가 없어서, "지금 이 창 = 1번 모니터"로 그대로 두고 2·3번 모니터용 창만 새로 엽니다. 불필요한 창을 하나 줄인 겁니다.

### 왜 창을 먼저 다 열고, 모니터 배치는 나중에 하는가
브라우저는 "사용자가 방금 클릭한 그 순간"에만 `window.open()`을 팝업 차단 없이 허용합니다(이걸 "user gesture"라고 부릅니다). 그런데 실제 모니터 좌표를 알아내는 `getScreenDetails()`는 비동기 함수라서, 이걸 먼저 `await`하고 나서 창을 열면 그사이 "방금 클릭했다"는 상태가 만료돼서 두 번째 창부터 팝업 차단에 걸릴 수 있습니다. 그래서 순서를 뒤집어서, 일단 대략적인 위치(화면을 반씩 나눈 좌표)로 창을 동기적으로 먼저 다 열어버리고, 그다음에 비동기로 실제 모니터 좌표를 알아내서 이미 열린 창들을 `moveTo`/`resizeTo`로 재배치합니다.

### 왜 지금 창이 있는 모니터를 자동으로 제외하는가
`window.screenLeft`/`screenTop`으로 지금 이 창이 어느 모니터에 떠 있는지 확인해서, 새 창 2개는 그 모니터를 뺀 "나머지" 모니터에만 배정합니다. 안 그러면 2번 모니터용 창이 이미 1번 모니터 위에 있는 대시보드 창과 겹쳐버릴 수 있기 때문입니다.

### CCTV 6개 vs 9개, `camOffset`/`fixedCount` 구조인 이유
일반 CCTV 그리드 뷰(사이드바에서 들어가는 화면)는 사용자가 6개/9개를 직접 고를 수 있어야 하지만, 3분할 모드의 2·3번 모니터는 화면을 꽉 채워야 하고 서로 겹치는 카메라가 없어야 합니다. 그래서 `fixedCount`(고정 개수, 토글 UI 숨김)와 `camOffset`(카메라 목록에서 몇 번째부터 보여줄지)이라는 두 개의 새 prop을 추가해서, 2번 모니터는 `offset=0`(1~9번 카메라), 3번 모니터는 `offset=9`(10~18번 카메라)로 서로 겹치지 않게 나눴습니다. 컴포넌트를 새로 만들지 않고 기존 `CctvGrid`를 그대로 재사용할 수 있게 한 이유이기도 합니다.

### kiosk 화면에서 카드 테두리가 안 보이던 버그, 왜 생겼는가
3분할 모드 화면(`?view=monitor2` 등)은 로그인 후 화면(`.app-shell`) 바깥에서 별도로 렌더링되는데, 카드 테두리·배경색이 전부 CSS 커스텀 프로퍼티(`--c-border`, `--c-bg2` 등)로 정의돼 있고 이 값들은 `.app-shell[data-theme]` 스코프 안에서만 선언돼 있었습니다. `.app-shell` 밖에서 렌더링되니 이 변수들이 전부 `undefined`가 돼서, 카드 배경/테두리가 투명하게 보였던 겁니다. 해결은 이 변수 세트를 `.kiosk` 클래스 자체에도 똑같이 선언해서, 어디서 렌더링되든 항상 값이 있게 만든 겁니다.

---

## 6. PDF 증거 리포트 — 왜 클라이언트에서 캡처하는 방식인가

지금 이벤트 카드의 "📄" 버튼은 서버에 요청을 보내는 게 아니라, 브라우저 안에서 화면 요소를 `html2canvas`로 이미지 캡처한 다음 `jsPDF`로 그 이미지를 PDF에 붙여넣는 방식입니다. 서버 왕복 없이 즉시 만들어지고, 백엔드에 PDF 생성 로직을 따로 안 둬도 된다는 장점이 있어서 빠르게 구현하기엔 이 방식을 썼습니다.

다만 이건 `b_report`(Python, ReportLab)가 만드는 "정식 증거 리포트"와는 완전히 별개의 경로입니다. `b_report` 쪽은 `POST /api/reports`로 DB에 등록까지 되는데, 대시보드의 PDF 버튼은 그냥 로컬에 파일을 다운로드시킬 뿐 서버에 아무 기록도 안 남습니다. 두 경로를 하나로 합칠지는 아직 결정 안 된 상태라, 지금 상태로 발표하면 "PDF 버튼 누른 게 실제 증거 리포트로 등록되냐"는 질문이 나올 수 있어서 팀원들도 이 차이를 알고 있는 게 좋습니다.

---

## 7. 파일럿 신청서 모달 — 왜 모달이고, 왜 저장 API가 없는가

버튼을 눌렀을 때 별도 페이지로 이동하는 대신 모달로 띄운 이유는, 사용자가 랜딩페이지의 맥락(가격 안내, 회사 정보)을 유지한 채로 문의를 작성할 수 있게 하기 위해서입니다. 페이지 이동은 사용자가 "뒤로 가기"를 눌러야 하는 이탈 지점을 하나 만드는데, 모달은 배경 클릭이나 X로 바로 닫고 원래 보던 화면으로 돌아갈 수 있습니다.

색상을 랜딩페이지 자체의 다크 톤이 아니라 로그인/회원가입 페이지의 라이트+블루 팔레트로 맞춘 건 요청받은 대로 반영한 것이고, 랜딩페이지 안에서도 "실제 데이터를 입력하는 폼"이라는 성격이 로그인/가입 폼과 비슷해서 그쪽 톤을 가져오는 게 자연스럽다고 판단했습니다.

**저장 API가 없는 이유는 단순합니다** — 이 문의를 받아서 어디에 저장할지(DB 테이블을 새로 만들지, 그냥 이메일로 발송만 할지), 누가 그 데이터를 볼지가 아직 정해지지 않았기 때문입니다. 화면 UI와 유효성 검사(필수값, 개인정보 동의)까지는 다 만들어뒀고, 뒷단은 방향이 정해지면 붙이면 됩니다.

---

## 8. GIS 지도의 `data/event.json` 폴링을 끈 이유

`map.js` 안에 이벤트를 받는 경로가 사실 두 개 있었습니다.

- **옛날 방식(`AiEventListener`)**: Python 스크립트가 `data/event.json`이라는 파일을 직접 갱신하면, 브라우저가 3초마다 그 파일을 GET으로 폴링해서 값이 바뀌었는지 확인하는 방식. 파일 기반이라 구현은 단순하지만, 폴링 특성상 최대 3초 지연이 있고 서버가 계속 불필요한 요청을 받습니다.
- **지금 방식(`AiWebSocketListener`)**: `realtime_anomaly.py`가 `server.js`로 이벤트를 보내면, `server.js`가 연결된 모든 브라우저에 WebSocket으로 즉시 broadcast. 지연이 거의 없고 폴링도 없습니다.

`map.js`는 두 경로를 둘 다 무조건 켜고 있었는데, 지금 파이프라인에서 `data/event.json`을 실제로 써주는 스크립트가 없어서(팀 전체 코드를 검색해서 확인) 있지도 않은 파일을 3초마다 계속 요청하며 콘솔에 404가 영구히 쌓이고 있었습니다. 실시간 경로(WebSocket)가 이미 정상 동작하고 있으니, 옛 파일 폴링 쪽을 끈 겁니다. 나중에 파일 기반 연동이 다시 필요해지는 경우를 대비해서 클래스 코드 자체는 지우지 않고 주석 처리만 해뒀습니다.

---

## 9. 관심대상 카운트가 "이벤트 수"가 아니라 "target 테이블"을 쓰는 이유

사이드바의 "추적 차량"/"관심 대상" 숫자는 **지나간 이벤트를 세는 게 아니라, 지금 실제로 추적 대상으로 등록돼 있는(target 테이블에 `status=ACTIVE`로 있는) 건수**를 셉니다.

이벤트 수로 계산하면(예: `UNREGISTERED_VEHICLE` 이벤트가 몇 건 왔는지), 같은 차량이 여러 카메라에 여러 번 찍히면 실제로는 차량 1대인데 숫자가 5, 10으로 부풀어 보입니다. 반대로 target 테이블 기준으로 세면, "지금 이 순간 몇 명/몇 대를 추적 중인가"라는 관제요원이 실제로 궁금한 질문에 정확히 답이 됩니다. DB 설계 자체가 "이벤트(발생 로그)"와 "관심대상(추적 상태)"을 처음부터 별개 테이블로 나눈 이유도 이 두 개념이 다르기 때문이고, 대시보드 카운트도 그 설계를 그대로 따른 겁니다.

---

## 10. `GATEWAY_API_KEY`/`JWT_SECRET` 기본값을 지운 이유

`application.yml`에 원래 이렇게 기본값이 박혀 있었습니다:

```yaml
gateway:
  api-key: ${GATEWAY_API_KEY:omecca-dev-key-2026}
app:
  auth:
    jwt-secret: ${JWT_SECRET:omecca-jwt-secret-change-this}
```

`${환경변수:기본값}` 문법이라 환경변수를 안 넣어도 `:` 뒤의 값이 자동으로 쓰였습니다. 편하긴 한데, 이 상태로 그대로 배포되면 "모두가 아는 공개된 값"이 운영 서버의 실제 인증키로 쓰이는 셈이라 문제입니다.

그래서 `DB_USERNAME`/`DB_PASSWORD`가 원래부터 쓰던 방식과 동일하게, 기본값을 아예 지워버렸습니다:

```yaml
gateway:
  api-key: ${GATEWAY_API_KEY}
app:
  auth:
    jwt-secret: ${JWT_SECRET}
```

`:기본값` 부분이 없으면 Spring은 그 값을 필수로 취급합니다 — 환경변수를 안 넣고 실행하면 `Could not resolve placeholder` 에러와 함께 서버가 아예 안 켜집니다. 즉 "설정을 깜빡해서 공개된 기본값으로 조용히 떠버리는" 상황 자체가 원천 차단됩니다.

이렇게 되면 내일 게이트웨이를 실행하는 사람은(팀원 전원 해당) 실행 전에 항상 아래 두 줄을 먼저 export 해야 합니다:

```bash
export GATEWAY_API_KEY=omecca-dev-key-2026
export JWT_SECRET=omecca-jwt-secret-change-this
```

지금 당장은 예전 기본값과 **똑같은 문자열**로 export하기 때문에 동작은 이전과 완전히 동일합니다 — 달라진 건 "자동으로 채워지던 값을 이제 직접 넣어줘야 한다"는 것뿐입니다. 나중에 실제 서버에 배포할 때는 이 값들을 `openssl rand -hex 32` 같은 걸로 만든 진짜 랜덤 값으로 바꿔서 export하면, 그때부터는 진짜 보안이 적용됩니다.

(참고: 처음엔 `application.yml`은 그대로 두고 `application-prod.yml`이라는 별도 배포 전용 프로필을 만드는 방법도 검토했지만, 파일이 하나 늘고 개념도 하나 늘어서 불필요하게 복잡해진다고 판단해 기존 파일을 직접 고치는 이 방식으로 정리했습니다.)

---

## 요약 — 한 줄씩

| 결정 | 이유 (한 줄) |
|---|---|
| JWT + X-API-Key 이원화 | 사람 로그인과 모듈 인증은 요구사항이 달라서 |
| WebSocket(STOMP+SockJS) | 새로고침 없는 실시간 반영 + 방화벽 환경 대응 |
| 공통필드 + meta 구조 | 이벤트 종류가 계속 늘어나도 DB/API 구조 안 바꾸려고 |
| Forza 버그 = 구간 시작 시점에 명시적 전환 | "지금 뭐가 재생 중인지" 추론 대신 확정적으로 판단하려고 |
| 3분할 = 창 2개만, 동기 오픈 후 재배치 | 불필요한 창 제거 + 팝업 차단 회피 |
| CCTV `camOffset`/`fixedCount` | 기존 컴포넌트 재사용하면서 2·3번 모니터 안 겹치게 |
| PDF는 클라이언트 캡처 | 서버 로직 없이 빠르게, 단 b_report와는 별개 경로 |
| 파일럿 모달 = 모달 + 저장 API 없음 | 이탈 방지 + 저장 방향 미정이라 UI만 우선 완성 |
| event.json 폴링 비활성화 | 실사용 경로(WebSocket)만 남기고 죽은 코드 정리 |
| 관심대상 카운트 = target 테이블 | "이벤트 로그"와 "추적 상태"는 다른 개념이라서 |
| `GATEWAY_API_KEY`/`JWT_SECRET` 기본값 삭제 | 공개된 기본값으로 조용히 뜨는 상황 자체를 막으려고 |
